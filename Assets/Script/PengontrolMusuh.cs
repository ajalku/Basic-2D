using UnityEngine;

public class PengontrolMusuh : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public void Kill()
    {
        Destroy(gameObject);
    }
}
